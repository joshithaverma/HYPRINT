fastapi==0.115.0
uvicorn[standard]==0.30.6
SQLAlchemy==2.0.35
python-multipart==0.0.9
pypdf==4.3.1
qrcode[pil]==7.4.2
requests==2.32.3

# Hardware integration notes:
# On Linux physical kiosk machines running CUPS, install libcups2-dev and pycups:
#   sudo apt install libcups2-dev && pip install pycups
# On Windows, native GDI / win32print is used.
# On Cloud / Vercel, simulation mode or cloud queue is used.
