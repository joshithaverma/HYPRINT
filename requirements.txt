fastapi==0.115.0
uvicorn[standard]==0.30.6
SQLAlchemy==2.0.35
python-multipart==0.0.9
pypdf==4.3.1
qrcode[pil]==7.4.2
requests==2.32.3

# Hardware integration — Linux kiosk PC only.
# Needs system packages first:  sudo apt install libcups2-dev
# Will not install on macOS/Windows dev machines; that's fine, the app runs
# in KIOSK_SIMULATE=1 mode without it.
pycups==2.0.4 ; sys_platform == "linux"
